### 16:41 — 項目重構與模組化
- **來源**：Joe (User)
- **內容**：將 `TAROT_DB` 提取至 `data/tarot-cards.json`，分離 CSS 至 `public/css/style.css`，分離 JS 至 `public/js/app.js`，並優化 `server.js` 與 `index.html`。
- **結果**：成功。專案架構轉變為更易維護的模組化結構，檔案體積大幅縮小。
- **Git**：6675e00

### 18:02 — 檔案名優化與解碼器解析能力強化
- **來源**：Joe (User)
- **內容**：將 `tarot-cards.json` 更名為 `tarot_db.json`，更新 `server.js`。強化 `app.js` 的 Markdown 解析器，支援標題、清單與粗體。整理前後端換行符號處理邏輯。
- **結果**：成功。解析器現在能正確渲染 Android_02 的專業解碼報告。
- **Git**：adfca33
